{
  "name": "signature-pieces",
  "version": "1.0.0",
  "description": "Create a simple drawing and generate a random fact.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "mongoose": "^4.5.5"
  }
}
